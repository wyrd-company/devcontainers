{
  "id": "t3code-server",
  "version": "2.0.0",
  "name": "T3 Code Server",
  "documentationURL": "https://github.com/wyrd-company/devcontainers/tree/main/src/features/t3code-server",
  "description": "Installs T3 Code as a pinned release runtime and runs it under the T3 Code service launcher as an s6-overlay service, so clients can update the server remotely and operators can update it in place.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "T3 Code version to install. With upstream, `latest` follows GitHub's latest upstream release. With a GitHub source, `latest` selects the greatest stable `server/*-wyrd.*` tag by SemVer precedence."
    },
    "packageSource": {
      "type": "string",
      "default": "",
      "description": "Optional GitHub repository source such as `github:wyrd-company/t3code`, or an npm package spec or tarball URL. Empty installs upstream T3 Code."
    },
    "releaseBaseUrl": {
      "type": "string",
      "default": "",
      "description": "Optional base URL the server downloads release archives from when a client asks it to update itself (`T3CODE_RELEASE_BASE_URL`). Empty derives it from the package source."
    },
    "port": {
      "type": "string",
      "default": "3773",
      "description": "Port exposed by the T3 Code server."
    },
    "host": {
      "type": "string",
      "default": "0.0.0.0",
      "description": "Interface to bind the T3 Code server to."
    },
    "serveMode": {
      "type": "string",
      "enum": ["", "web", "desktop"],
      "default": "",
      "description": "Optional T3 runtime mode (`T3CODE_MODE`). Set to `web` for the web runtime. Empty preserves the T3 CLI default."
    },
    "serviceUser": {
      "type": "string",
      "default": "automatic",
      "description": "User account to run the service as. `automatic` prefers the remote user, container user, `vscode`, then `root`."
    },
    "dnsName": {
      "type": "string",
      "default": "",
      "description": "Optional fully qualified DNS name exposed through the Caddy Feature, such as `t3.dev-environment.example.test`."
    }
  },
  "dependsOn": {
    "ghcr.io/devcontainers/features/node:2": {
      "version": "24"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils",
    "ghcr.io/wyrd-company/devcontainers/caddy"
  ]
}
